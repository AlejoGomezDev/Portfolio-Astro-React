---
interface Skill {
  name: string;
  icon: string;
  alt: string;
}

export interface Props {
  skill: Skill;
}

const { skill } = Astro.props;
---

<div class="flex flex-col p-2">
  <h5 class="pb-1">{skill.name}</h5>
  <img 
    src={skill.icon} 
    alt={skill.alt} 
    class="w-auto h-12 object-contain" 
  />
</div>
